Module ModuloAreas

    Function CalcularAreaTriangulo(Base As Double, Altura As Double)
        Dim Area As Double
        Area = (Base * Altura) / 2
        Return Area
    End Function

End Module
